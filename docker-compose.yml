version: "3.8"
services:
  mongodb:
    image: mongo:latest
    container_name: paycom-db
    volumes:
      - paycomgwdb:/var/lib/mysql
    restart: always
  api:
    build:
      context: .
    image: paycom-api:latest
    container_name: paycom-api
    depends_on:
      - mongodb
    ports:
      - 1338:1338
    restart: always

volumes:
  paycomgwdb:
